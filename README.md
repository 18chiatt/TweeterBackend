# TweeterBackend
Contains code for AWS lambda handlers 

Currently there is no backend, so all of the handlers pull from a static generated JSON file stored in an S3 bucket. This will be updated to use a real database soon
